SET(SOURCE_FILES
  lsqlite3.c
)

INCLUDE_DIRECTORIES(
  ../lua
  ../sqlite
)

ADD_LIBRARY(lsqlite3_static STATIC ${SOURCE_FILES})

SET_TARGET_PROPERTIES(lsqlite3_static PROPERTIES OUTPUT_NAME "lsqlite3")
SET_TARGET_PROPERTIES(lsqlite3_static PROPERTIES CLEAN_DIRECT_OUTPUT 1)
