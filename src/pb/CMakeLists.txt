SET(SOURCE_FILES
  pb.c
)

INCLUDE_DIRECTORIES(
  ../lua
)

ADD_LIBRARY(pb_static STATIC ${SOURCE_FILES})

SET_TARGET_PROPERTIES(pb_static PROPERTIES OUTPUT_NAME "pb")
SET_TARGET_PROPERTIES(pb_static PROPERTIES CLEAN_DIRECT_OUTPUT 1)
